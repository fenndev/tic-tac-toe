// Game Manager

const GameManager = (() => {

})();

// Display Manager

const DisplayManager = (() => {

})();

// Player

const Player = (name) => {
    let symbol = "";
    let wins = 0;
    return { name, symbol, wins };
};